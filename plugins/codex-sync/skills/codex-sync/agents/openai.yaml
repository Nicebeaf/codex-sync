interface:
  display_name: "Codex Sync"
  short_description: "Safe Skills sync and dependency readiness"
  default_prompt: "Use $codex-sync with my private shared folder, run a safe quick sync, then audit local Skill dependencies and show the exact dependency Plan ID and commands before asking whether to install them."
