interface:
  display_name: "Codex Sync"
  short_description: "Private Skills sync for macOS and Windows"
  default_prompt: "Use $codex-sync with my own private shared folder. For routine use, run sync-now so the latest handoff receipt is verified automatically; stop before changing selected files if there is a conflict, and never include credentials or live Codex state."
